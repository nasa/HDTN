get_filename_component(BPOVERENCAPLIB_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

find_dependency(HDTNUtil REQUIRED)
find_dependency(TelemetryDefinitions REQUIRED)
find_dependency(LoggerLib REQUIRED)

if(NOT TARGET HDTN::BpOverEncapLib)
    include("${BPOVERENCAPLIB_CMAKE_DIR}/BpOverEncapLibTargets.cmake")
endif()

set(BPOVERENCAPLIB_LIBRARIES HDTN::BpOverEncapLib)
